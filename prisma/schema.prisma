generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Module-owned persistence models are added only when their bounded contexts
// receive explicit implementation prompts.
